import { expect } from "@jest/globals";
import { sum } from "./lib";

// describe("sum", () => {
//   it("should sum two positive integers", () => {
//     expect(sum(2, 3)).toEqual(5);
//   });
// });
